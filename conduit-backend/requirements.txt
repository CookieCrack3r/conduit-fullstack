Django==1.10.5
django-cors-middleware==1.3.1
django-extensions==1.7.1
djangorestframework==3.4.4
PyJWT==1.4.2
six==1.10.0

# --- Added for the containerised setup ---------------------------------------
# psycopg2 2.9 changed datetime handling in a way that breaks Django < 3.1,
# so this pin is deliberate and must not be bumped.
psycopg2-binary==2.8.6
# WhiteNoise 4.0 dropped Django 1.10; 3.3.1 is the last compatible release.
whitenoise==3.3.1
# WSGI server used as the container entrypoint (no dev server in containers).
gunicorn==20.1.0

