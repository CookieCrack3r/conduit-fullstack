# Shell scripts must keep LF endings, otherwise the shebang breaks inside the
# Linux containers when the repository is cloned on Windows.
*.sh text eol=lf
entrypoint.sh text eol=lf
Dockerfile text eol=lf
*.yaml text eol=lf
*.yml text eol=lf
